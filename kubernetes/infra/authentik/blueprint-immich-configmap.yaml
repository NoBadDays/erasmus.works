apiVersion: v1
kind: ConfigMap
metadata:
  name: authentik-blueprint-immich
  namespace: apps
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
data:
  immich.yaml: |
    version: 1
    metadata:
      name: Immich OAuth application
      labels:
        blueprints.goauthentik.io/instantiate: "true"
        blueprints.goauthentik.io/description: "GitOps-managed Immich OIDC application and provider."
    entries:
      - model: authentik_providers_oauth2.oauth2provider
        id: immich-provider
        identifiers:
          name: Immich OAuth
        attrs:
          name: Immich OAuth
          authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-explicit-consent]]
          invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
          client_type: confidential
          client_id: immich-erasmus-works
          client_secret: !Env IMMICH_OAUTH2_CLIENT_SECRET
          redirect_uris:
            - matching_mode: strict
              url: app.immich:///oauth-callback
            - matching_mode: strict
              url: https://immich.erasmus.works/auth/login
            - matching_mode: strict
              url: https://immich.erasmus.works/user-settings
          property_mappings:
            - !Find [authentik_providers_oauth2.scopemapping, [managed, goauthentik.io/providers/oauth2/scope-openid]]
            - !Find [authentik_providers_oauth2.scopemapping, [managed, goauthentik.io/providers/oauth2/scope-email]]
            - !Find [authentik_providers_oauth2.scopemapping, [managed, goauthentik.io/providers/oauth2/scope-profile]]
          signing_key: !Find [authentik_crypto.certificatekeypair, [name, authentik Self-signed Certificate]]
      - model: authentik_core.application
        identifiers:
          slug: immich
        attrs:
          name: Immich
          slug: immich
          launch_url: https://immich.erasmus.works
          provider: !KeyOf immich-provider
