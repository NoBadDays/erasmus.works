apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: volsync-repository
  namespace: app-docmost
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Gi
  storageClassName: ""
  volumeName: volsync-backups-app-docmost
